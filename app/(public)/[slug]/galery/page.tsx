import GuestMemoriesGalleryPage, { generateMetadata as baseGenerateMetadata } from "../memories/page";

export const dynamic = "force-dynamic";

export const generateMetadata = baseGenerateMetadata;

export default GuestMemoriesGalleryPage;
